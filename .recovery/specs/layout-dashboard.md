Findings from .recovery/app.pretty.js:

(1) Layout/sidebar:
- OW() at lines 18837-18947 builds sidebar. Mobile overlay: `fixed inset-0 z-40 bg-black/50 md:hidden`; aside classes `fixed md:static inset-y-0 right-0 z-50 w-64 bg-sidebar border-l border-sidebar-border flex flex-col h-full no-print transform transition-transform duration-300 md:transform-none ${open?"translate-x-0":"translate-x-full"} md:translate-x-0` (18888-93). Header h-16, px-6, bottom border; icon `w-6 h-6 text-sidebar-primary ml-3`, title exact `نظام عبور الذكي`; mobile close aria-label exact `إغلاق` (18894-908). Nav `flex-1 overflow-y-auto py-4 px-3 space-y-1` (18909-11). Active detection: pathname exactly href, or non-root href prefix; active classes `bg-sidebar-accent text-sidebar-accent-foreground font-medium`, icon `text-sidebar-primary`; inactive `text-sidebar-foreground/70 hover:bg-sidebar-accent/50 hover:text-sidebar-foreground` (18911-23).
- Base nav array (18858-74): `/` `لوحة القيادة` icon FH (`layout-dashboard`); `/umrah` `عملاء العمرة` icon Vr; `/visas` `تأشيرات أخرى` icon IH; `/archive` `الأرشيف العام` icon eS. If role `owner` OR `provider`, append `/office` `بيانات المكتب` icon po and `/statement` `كشف الحساب` icon $$. If role `provider`, append `/provider` `إدارة المزوّد` icon sO (18875-87). Thus sub role receives only base four.
- Footer p-4 top border; username `a?.username`; role exact labels: provider `المزوّد`, owner `الحساب الرئيسي`, otherwise `حساب فرعي` (18925-35). Logout outline destructive full-width button classes `w-full justify-start text-destructive hover:bg-destructive/10 border-destructive/20 hover:text-destructive`, exact `تسجيل الخروج`; calls `/api/auth/logout` mutation, on success clears auth, closes sidebar, routes `/login`; error toast title `خطأ`, description `حدث خطأ أثناء تسجيل الخروج` (18845-57, 18936-44; logout endpoint 18185-93).
- No office name/logo display is present in this sidebar/header: branding is hardcoded `نظام عبور الذكي`. Office config is separate first-run modal/layout flow (PX lines 20618+): owner with unconfigured office opens CX; office fields/API elsewhere. Search finds office strings around 20132+, 20277+, 20386+, 20438+ and login/receipt screens, not OW sidebar.
- PX shell (20649-84): `flex h-screen overflow-hidden bg-background`; content flex column; MX banner before mobile header; mobile header `md:hidden flex items-center gap-3 h-14 px-4 border-b border-border bg-card ...`, menu button aria-label exact `القائمة`, brand exact `نظام عبور الذكي`; main `flex-1 overflow-y-auto w-full oboor-canvas`.
- MX offline/outbox (20554-20615): online state + pending count. Online with pending: amber `bg-amber-50 text-amber-900 border-amber-200`, spinner while uploading and exact `جارٍ رفع المعاملات المعلّقة...`; otherwise `${n} معاملة بانتظار الرفع`; outline sm button `رفع الآن` (h-7, mr-auto). Upload success toast exact template `تم رفع ${h} معاملة معلّقة بنجاح`; failures title `تعذر رفع: ${v.label}`, description error. Offline: destructive classes `bg-destructive/10 text-destructive border-destructive/20`, exact `لا يوجد اتصال بالإنترنت — يمكنك متابعة العمل، وستُرفع المعاملات الجديدة تلقائياً عند عودة الاتصال.` plus if pending `(${n} بانتظار الرفع)`. Banner hidden when online and count zero. Automatically uploads on online event and mount; tracks online/offline events.

(2) Dashboard page rve() lines 47944-48321:
- Fetches four GET endpoints concurrently: `/api/dashboard/stats` (KY), `/api/dashboard/monthly` (ZY; no query params in bundle), `/api/dashboard/agents` (rW), `/api/dashboard/outstanding` (lW), lines 18509-618. Loading skeleton: `p-4 sm:p-6 md:p-8 space-y-8 animate-pulse`; heading placeholder h-8 w-1/4; four KPI placeholders grid `grid grid-cols-1 md:grid-cols-2 lg:grid-cols-4 gap-6`, each h-32 rounded-xl; chart placeholder h-96.
- Main wrapper `p-4 sm:p-6 md:p-8 max-w-7xl mx-auto space-y-8 animate-in fade-in slide-in-from-bottom-2 duration-500`.
- Hero: `oboor-hero ... rounded-2xl px-6 py-7 md:px-8 md:py-8 text-white shadow-lg`; gold `text-[hsl(40,66%,60%)]`; chip; heading exact `لوحة القيادة`; subtitle exact `نظرة عامة على أداء المكتب والإحصائيات المالية.` (47978-48002).
- KPI grid 1/2/4 columns, gap-6. Card 1 label `إجمالي عملاء العمرة`, icon Vr (users/umrah), value stats.umrahTotal formatted om(); sublabels green dot `داخل: ` + umrahInsideKsa and red dot `خارج: ` + umrahOutsideKsa. Top stripe `bg-gradient-to-l from-primary to-accent`.
- Card 2 label `إجمالي التأشيرات الأخرى`, icon xm, value visasTotal, subtext `التأشيرات غير العمرة`; stripe `from-accent to-primary`.
- Card 3 label `إجمالي الأرباح`, icon q$ (wallet/profit), green-tinted `bg-green-50/40 dark:bg-green-950/20`, stripe green-500→emerald-400; value totalProfit green; subtext `العمرة: ` umrahTotalProfit ` | أخرى: ` visasTotalProfit; icon green gradient.
- Card 4 label `مبالغ مستحقة (تأشيرات)`, icon MH, amber-tinted `bg-amber-50/40 dark:bg-amber-950/20`, stripe amber-500→yellow-400; value visasTotalClientBalance amber; subtext `بواقي على الوكلاء: ` + visasTotalAgentBalance; icon amber→orange gradient.
- Chart/outstanding row `grid grid-cols-1 lg:grid-cols-3 gap-8`; chart card spans `col-span-1 lg:col-span-2`. Heading exact `الأداء الشهري`; description `عدد التأشيرات المصدرة حسب الشهر`. Chart is Recharts-style ResponsiveContainer Xre + BarChart Jpe, 350px, `dir="ltr"`, margins top20/right30/left20/bottom5. Monthly mapping fields: `{name: month/year, "عمرة (عدد)": umrahCount, "أخرى (عدد)": visasCount, "ربح العمرة": umrahProfit, "ربح الأخرى": visasProfit}`. Rendered series are count bars only: `عمرة (عدد)` and `أخرى (عدد)`; gradient barUmrah uses `hsl(var(--chart-1))` opacity .95→.6; barOther uses `hsl(var(--chart-2))` opacity .95→.65; radius top corners [6,6,0,0], maxBarSize 44. Grid dashed `3 3`, border color; axes muted foreground fontSize12; tooltip card background/border, radius8, cursor muted 0.4.
- Outstanding card class border red (`border-red-200 dark:border-red-900/50`), header red tint/bottom border. Heading exact `تنبيهات البواقي` with B$ alert icon; description exact `عملاء عليهم مستحقات مالية (أكثر من 0)`. Table scroll max-h-[350px], headers `العميل`, `الباقي`; each row clientName (font-medium text-sm), phone (text-xs muted), balance left bold red `nt(h.clientBalance)`. Empty exact `لا يوجد عملاء عليهم مستحقات مالية.` with centered muted p-8 and icon.
- Agent card full width below: heading `أداء الوكلاء`; description `إحصائيات المبيعات والأرباح حسب الوكيل`. Horizontal overflow table headers `الوكيل`, `عمرة (عدد)`, `تأشيرات أخرى (عدد)`, `إجمالي الربح`; rows agent or exact fallback `بدون وكيل`, count cells outline badges font-mono, profit left bold green. Empty exact `لا توجد بيانات متاحة للوكلاء` (48263-321).