Offline/PWA spec extracted.

OUTBOX (.recovery/app.pretty.js:18707-18793): localStorage key exactly `oboor-outbox-v1`; query persistence key separately `oboor-query-cache-v1`. `qm()` parses key or `[]`, catches parse errors; `uS(e)` JSON.stringify stores and notifies subscribers (`Of` Set). Queue item created by `G3(kind,payload,label)` shape: `{ id: `${Date.now()}-${Math.random().toString(36).slice(2,8)}`, kind: e, payload: t, label: n, queuedAt: new Date().toISOString() }`. `Mb()` is queue length; `wW` subscribes; in-flight boolean `Am` prevents concurrent flush.

Flush `SW()` (18750-18793): if already flushing returns `{uploaded:0,failed:[]}`. Iterates snapshot `[...n]` of current queue in insertion order. Endpoint is `/api/umrah` for `kind === "umrah"`, otherwise `/api/visas`; POST, `credentials:"include"`, header `Content-Type: application/json`, body `{...r.payload, clientRequestId:r.id}`. Network/fetch exception: `break` (retains current and remaining). After response rereads queue. `o.ok`: count uploaded and remove item by id. 4xx except 401/429 (i.e. 400-499, excluding those) parses JSON fallback `{}`, records `{label:r.label,error:l.error || `خطأ ${o.status}`}`, removes item. Everything else breaks: thus 5xx, 401, 429, other non-ok are retained for later retry. `finally` clears in-flight and notifies.

Offline UI/trigger (20554-20615): state starts `navigator.onLine`, queue count, flushing. `online` event sets online and calls flush; `offline` sets offline. Effect also calls flush immediately. Manual button `رفع الآن`; status strings: `جارٍ رفع المعاملات المعلّقة...`, `${n} معاملة بانتظار الرفع`, `لا يوجد اتصال بالإنترنت — يمكنك متابعة العمل، وستُرفع المعاملات الجديدة تلقائياً عند عودة الاتصال.`, `(${n} بانتظار الرفع)`. New records branch on `navigator.onLine`: offline calls `G3("umrah", E, E.clientName)` or equivalent visa path and toast `حُفظت مؤقتاً على الجهاز` / `ستُرفع تلقائياً عند عودة الإنترنت` (umrah at 50897-50910; visa analogous around 51507). No IndexedDB/localforage found; outbox and TanStack query cache use localStorage.

CACHE WIPE/auth: `K3()` (18744-18749) removes localStorage `oboor-query-cache-v1` and `await caches.delete("api-cache")`, swallowing cache errors; it does NOT remove outbox. Logout calls K3 then redirects `/login` on successful `/api/auth/logout` (18837-18849). Login/logout API keys/routes are `/api/auth/login`, `/api/auth/logout`; SW explicitly excludes `/api/auth/` from API caching. Query persister uses `window.localStorage`, key `oboor-query-cache-v1`, maxAge `1e3*60*60*24*7` (7 days) (54610-54620).

SERVICE WORKER (.recovery/sw.js:1): Workbox module `./workbox-efbd304a`; `self.skipWaiting(); self.clientsClaim()`. Precache entries: `registerSW.js` rev `1872c500de691dce40960bb85481de07`; `oboor-logo.ico` rev `4e2171ec72311d90faf0b444eb078e45`; `oboor-hero.png` rev `2cc35da8549b85cb9416d1241966481d`; `index.html` rev `15c7bf9cad38ad39023ce4758a6e5c8c`; `favicon.svg` rev `1df424bb74dd3799fd103a4226daeaf3`; `assets/index-KF4Fe9vg.js` revision null; `assets/index-C0P2oKEd.css` revision null; `manifest.webmanifest` rev `83250b2a0e20b88fa7299ce954052ea6`. Calls `cleanupOutdatedCaches()`. Navigation fallback is `createHandlerBoundToURL("/index.html")` with denylist `/\/api\//`, so API navigations are excluded. Runtime route only GET where pathname includes `/api/` AND does not include `/api/auth/`; NetworkFirst cache `api-cache`, `networkTimeoutSeconds:5`, maxEntries 200, maxAge 604800 seconds (7 days), cacheable statuses `[200]`. No explicit registerSW invocation found in app.pretty.js; generated `registerSW.js` is merely precached (likely external bootstrap).

MANIFEST (.recovery/manifest.json; SW precache names `manifest.webmanifest`): `{"name":"نظام عبور الذكي","short_name":"عبور","start_url":"/","display":"standalone","background_color":"#ffffff","theme_color":"#0f172a","lang":"ar","scope":"/","dir":"rtl"}`.

RECEIPT page: route guard at 20644 renders receipt route without shell; route `/receipt/:id` (around 54589-54607), query `?type=umrah` selects umrah vs visa. Receipt component around 52170-52423 fetches record (separate umrah/visa query), office logo via `RO().then(j=>g(j.officeLogo))`, office config via `gd()`. Loading text is spinner; missing record shows `البيانات غير موجودة` and `العودة للرئيسية`. Buttons both call `window.print()`: `طباعة`, `تصدير PDF`; back is `العودة` to `/umrah` or `/visas`; print controls wrapper `no-print`, card print classes.

Receipt verbatim/content: office fallback `مكتب اللواء الغربي`, subtitle fallback `للنقل والسفريات والسياحة`; logo alt `شعار المكتب`; date label `التاريخ: ` formatted by `La`; ID `رقم السند: #` + six-digit zero-padded id; support `للتواصل والدعم`, fallback phone `771436479`; title `سند قبض ` + `(عمرة)` or `(تأشيرة)`. Fields `اسم العميل`, `رقم الجوال`, `رقم الجواز`, `الوكيل`; columns `الحركة`, `التفاصيل`, `المدفوع`, `المتبقي`; transaction is `تأشيرة عمرة` or visaType; details `تاريخ الإصدار: ` + date and optional `جهة المعاملة: `; total `الإجمالي:`; optional `ملاحظات:`; signatures `توقيع المستلم (المكتب)`, `توقيع المسلم (العميل)`; footer `نسعد بخدمتكم ونتمنى لكم رحلة موفقة`, `تمت الطباعة بواسطة نظام عبور الذكي`. Amounts: umrah `salePrice`, remaining 0; visa `receivedFromClient`, remaining `clientBalance`; total always `salePrice`.