Complete login/auth spec from .recovery/app.pretty.js:

LOGIN component `kQ` lines 25528-25661. Outer `<div>` is `min-h-screen bg-background flex flex-col md:flex-row`, `dir="rtl"`; at mobile stacked, md+ two columns. First child `PQ` is hero: `hidden md:flex flex-1 ... bg-[#0a1628] ...`, dark radial-gradient background plus decorative full-size SVG gold lines/glow (not a photographic hero image). PQ calls `RO()` on mount; RO GETs `${pL}/api/settings/branding` with `credentials:'include'`, returns `{officeName:t.officeName??'', officeLogo:t.officeLogo??''}` (empty values on non-OK/error). PQ uses only `officeLogo`: if present, `<img src={officeLogo} alt="شعار المكتب" class="h-28 w-28 ... object-contain ...">`; otherwise inline shield/gold SVG. Hero copy: `OBOOR`; `للتقنية والحلول الرقمية`; `عبور: زيادة رقمية، وجهة في الإنجاز`; four feature labels: `نظام أمان وحماية`, `إدارة الطلبات`, `التقارير والإحصاءات`, `الحسابات المالية`; phone `771436479` (dir=ltr).

Right form column: `flex-[0.85] bg-card flex flex-col justify-center p-6 sm:p-8 md:p-16 lg:p-20 md:border-r border-border`; inner `w-full max-w-sm mx-auto`. Static login logo is `/basmah-logo.jpeg` (base `RQ='/'`), 80x80 rounded bordered white container; img alt exactly `بصمة إلكترونية`, object-contain. Heading `تسجيل الدخول`; subtitle `الرجاء إدخال بيانات الاعتماد الخاصة بك`. React-hook-form fields/schema: username min(1, `اسم المستخدم مطلوب`), password min(1, `كلمة المرور مطلوبة`). Labels `اسم المستخدم`, `كلمة المرور`; placeholders `أدخل اسم المستخدم`, `أدخل كلمة المرور`; both inputs dir=ltr, h-12, autocomplete `username` / `current-password`; password type=password. Validation messages rendered by FormMessage. Submit button full width h-12; pending disabled and shows spinner, otherwise label `دخول`.

Activation/footer: WhatsApp anchor target `_blank`, rel `noreferrer`, href exactly `https://wa.me/967781332742?text=%D8%A3%D8%B1%D8%BA%D8%A8%20%D8%A8%D8%AA%D9%81%D8%B9%D9%8A%D9%84%20%D8%A3%D9%88%20%D8%AA%D8%AC%D8%AF%D9%8A%D8%AF%20%D8%A7%D8%B4%D8%AA%D8%B1%D8%A7%D9%83%20%D9%81%D9%8A%20%D9%86%D8%B8%D8%A7%D9%85%20%D8%B9%D8%A8%D9%88%D8%B1`; visible text `لتفعيل أو تجديد الاشتراك — واتساب ` + `781332742` (ltr). Footer: `جميع الحقوق محفوظة © 2026 لـ بصمة إلكترونية`.

Login mutation: POST `/api/auth/login`, JSON `body: JSON.stringify({username,password})`, Content-Type application/json (lines 18156-18164). On success: `K3()` removes localStorage `oboor-query-cache-v1` and attempts Cache API delete `api-cache`; clears query client; invalidates query key `['/api/auth/me']`; redirects via router setter to `/`. On error status `u?.response?.status ?? u?.status`, server error `u?.response?.data?.error`: toast destructive title is status===403 ? `الحساب منتهي` : `فشل تسجيل الدخول`; description status===403 ? server error or `انتهت صلاحية الحساب. تواصل مع المزوّد.` : `اسم المستخدم أو كلمة المرور غير صحيحة`. Thus 401 and all non-403 failures use generic invalid-credentials message; expired/account-ended is 403.

Auth/useAuth guard: query helper `ov()` runs GET `/api/auth/me` with query key `['/api/auth/me']` (credentials behavior inherited by request helper). `zge` consumes `{data,isLoading,isError}=ov()`: on `isError`, effect redirects `/login`; while loading renders centered spinner; when data exists renders app shell `Uge`, passing `isProvider: e.role === 'provider'`. Router `Fge`: `/login` directly renders kQ; every other route is wrapped by zge, so unauthenticated API-me failure redirects to login. No separate token/localStorage auth key is used; auth is cookie/session based. localStorage keys observed: `oboor-query-cache-v1` (persisted query cache, maxAge 7 days, cleared after login/logout) and `oboor-outbox-v1` (offline queued mutations; login flow does not read/write it).