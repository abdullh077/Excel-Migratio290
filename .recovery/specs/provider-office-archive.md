Bundle is at attached_assets/recovery/bundle.js (117 very long lines; requested .recovery/app.pretty.js is not present). Findings/spec from bundle:

PROVIDER nav label exactly «إدارة المزوّد», route /provider, provider-only. API helpers: GET /api/provider/accounts; POST /api/provider/owners body form; POST /api/provider/subs body form; PATCH /api/provider/accounts/{id}/expiry body {expiresAt}; PATCH .../{id}/password body {password}; PATCH .../{id}/username body {username}; DELETE .../{id}; GET /api/provider/backups; POST /api/provider/backup; download URL /api/provider/backups/{encodeURIComponent(name)}. Tabs: «الحسابات الرئيسية», «الحسابات الفرعية», «النسخ الاحتياطي». Main subtitle: «كل مكتب له حساب رئيسي واشتراك بمدة محددة. الحسابات الفرعية تتبع اشتراكه وتُدار من تبويبها الخاص.» Create-owner dialog title «مكتب جديد (حساب رئيسي)» fields «اسم المستخدم», «كلمة المرور», «تاريخ مخصص», «تاريخ الانتهاء»; submit «إنشاء الحساب», cancel «إلغاء». Sub dialog title «حساب فرعي جديد», explanatory text «الحساب الفرعي يشارك نفس بيانات المكتب الذي يتبعه، وينتهي اشتراكه مع الحساب الرئيسي في نفس اللحظة.» field «يتبع الحساب الرئيسي», select placeholder «اختر المكتب», then «اسم المستخدم», «كلمة المرور», «تاريخ مخصص». Account row actions «تغيير الاسم», «تغيير كلمة المرور», likely expiry extension action; dialogs titles «تغيير الاسم», «تغيير كلمة المرور», delete confirmation title «حذف المكتب». Success toasts: «تم إنشاء حساب المكتب», «تم إنشاء الحساب الفرعي», «تم تغيير كلمة المرور», «تم تغيير الاسم».
Backup UI is definitely present: heading «النسخ الاحتياطي والاستعادة»; text «احفظ نسخة من بياناتك في مكان آمن، أو استعد نسخة سابقة عند الحاجة.» buttons «أخذ نسخة احتياطية» and «استعادة»; empty «لا توجد نسخ بعد — ستُنشأ أول نسخة تلقائياً.»; card heading «النسخ الاحتياطي», text «نسخة تلقائية تُحفظ يومياً عند أول دخول لك، وتقدر تنشئ نسخة الآن أو تحمّل أي نسخة سابقة.» button «نسخة احتياطية الآن». Toasts «تم حفظ النسخة الاحتياطية بنجاح», «تعذّر أخذ النسخة», «تم إنشاء النسخة الاحتياطية».

OFFICE route /office/nav «بيانات المكتب». API GET/PUT (?) /api/settings/office; branding separate fetch POST/PUT /api/settings/branding (logo). Logo label «شعار المكتب (يظهر في شاشة تسجيل الدخول)», file accept image/*, button «رفع صورة», remove icon; success «تم حفظ الشعار», delete «تم حذف الشعار». UI says 1.5MB limit in nearby helper (bundle search did not expose exact text); image is read as base64/data URL before branding request. Office fields exact: «اسم المكتب *», «الهاتف الأساسي», «هاتف إضافي», «العنوان». WhatsApp textarea labels «قالب رسالة واتساب — العمرة» variables «{office} · {name} · {days}»; «قالب رسالة واتساب — تأشيرات أخرى» variables «{office} · {name} · {visaType}». Save «حفظ», cancel «إلغاء», toast «تم حفظ بيانات المكتب». First-run dialog title exactly «مرحباً — إعداد بيانات المكتب»; text «أدخل بيانات مكتبك لتظهر في السندات ورسائل واتساب. يمكنك تعديلها لاحقاً من «بيانات المكتب».»; normal title «بيانات المكتب» and text «تظهر هذه البيانات في سندات القبض ورسائل واتساب.».

ARCHIVE route /archive title «الأرشيف العام» subtitle «سجل شامل لجميع التأشيرات (عمرة وتأشيرات أخرى) للقراءة فقط.» Search placeholder «بحث شامل بالاسم، الجواز، الجوال...». Table columns exactly: «م», «العميل», «الوكيل», «التاريخ», «النوع/المصدر», «الشراء», «البيع», «الربح», «المصدر». Client cell shows clientName and `passportNumber | phone`; date formatted issueDate. Loading «جاري تحميل الأرشيف...», empty title «لا توجد سجلات في الأرشيف», hint «تظهر هنا المعتمرون المنتهية مدتهم وكل التأشيرات الأخرى.»