Complete STATEMENT spec in .recovery/app.pretty.js:

Component/state/data: Oge at lines 52752–53515. Loads agents via GET /api/statement/agents, ledger GET /api/statement/ledger, summary GET /api/statement/summary; agent drilldown GET /api/statement/agents/{id} enabled when selected. On mutations invalidates all three plus selected agent query (52756–52789). API helpers exact lines 48341–48371: POST agents body {name, phone}; PUT agents/{id} same; DELETE agents/{id}; POST agents/{id}/payments body {amount:Number(_), direction:x, notes:j.trim()||undefined}; DELETE /api/statement/payments/{id}; POST /api/statement/ledger body {type:T, amount:Number(E), description:R.trim()}; DELETE /api/statement/ledger/{id}; GET /api/statement/agent-names. All requests credentials include, JSON content type for writes.

Page heading (52863–52878): "كشف الحساب"; subtitle "حسابات الوكلاء، الدخل والنفقات، والملخص الشهري"; RTL. Tabs (52879–52900), default agents: value agents label "الوكلاء"; value ledger label "الدخل والنفقات"; value summary label "الملخص الشهري".

Agents section (52900–53008): intro "سجّل وكلاءك هنا، واختر اسم الوكيل عند إضافة أي معاملة ليظهر في كشفه."; button "وكيل جديد". Table columns exact: "الوكيل", "الهاتف", "المعاملات", "إجمالي البيع", "الربح", "الرصيد", "إجراءات". Agent fields rendered: ie.name, ie.phone || "-", ie.transactions, nt(ie.totalSales), nt(ie.profit), balance component PI(balance: ie.balance). Actions: "كشف الحساب" drilldown, edit aria-label="تعديل", delete aria-label="حذف". Empty: title "لا يوجد وكلاء بعد", hint "أضف وكيلاً لبدء تتبع حساباته". Balance display component lines 52735–52749: positive => badge "عليه " + nt(e); negative => green badge "له " + nt(-e); zero => outline "مسدَّد". (Thus positive balance is amount due from agent; negative owed to agent.)

Add/edit agent dialog (53208–53255): title edit template `تعديل الوكيل — ${a.name}` or "وكيل جديد". Fields exact: label "اسم الوكيل", placeholder "مثال: وكيل صنعاء"; label "الهاتف (اختياري)", placeholder "7xxxxxxxx". Save requires name trimmed length >=2; button "حفظ"; cancel "إلغاء". Create POST payload {name:l.trim(), phone:f.trim()||undefined}; edit PUT payload {name:l.trim(), phone:f.trim()} (phone may be empty string on edit). Success toasts "تم تعديل الوكيل"/"تم إضافة الوكيل".

Delete dialog (53256–53284): title `حذف الوكيل — ${p?.name}`; exact warning "سيُحذف الوكيل ودفعاته من كشف الحساب. معاملاته السابقة تبقى محفوظة باسمه."; buttons "إلغاء", destructive "حذف". Success toast "تم حذف الوكيل".

Agent detail dialog (53285–53515), title `كشف حساب — ${I?.agent.name ?? ""}`. Loading spinner while V || !I. Summary cards exact labels/fields: "المعاملات" => I.totals.count; "إجمالي البيع" => nt(I.totals.totalSales); "قبضنا منه" => nt(I.totals.paidFrom); "الرصيد" => PI(balance:I.totals.balance).

Payment recording: direction selector label "الاتجاه", exact values from_agent label "قبضنا من الوكيل" and to_agent label "دفعنا للوكيل"; state default from_agent. Amount numeric placeholder "0"; optional notes label "ملاحظة (اختياري)"; button "تسجيل دفعة". Disabled unless Number(amount) and not pending. POST payload exact {amount:Number(_), direction:x, notes:j.trim()||undefined}. Success toast "تم تسجيل الدفعة".

Payments list appears only if I.payments.length > 0; heading "الدفعات"; columns "التاريخ", "الاتجاه", "المبلغ", "ملاحظة", blank action. Fields: La(ie.paidAt), direction from_agent => "قبضنا منه", otherwise "دفعنا له"; nt(Number(ie.amount)); ie.notes || "-"; delete aria-label="حذف". Delete uses DELETE /api/statement/payments/{id}, success toast "تم حذف الدفعة".

Transactions drilldown: heading `المعاملات (${I.totals.count})`; columns "التاريخ", "النوع", "العميل", "البيع", "الشراء", "الربح". Fields La(ie.date), kind umrah => "عمرة" else "تأشيرة", ie.clientName, nt(ie.sale), nt(ie.purchase), nt(ie.sale-ie.purchase). Empty title "لا توجد معاملات لهذا الوكيل", hint "اختر اسمه في حقل الوكيل عند إضافة معاملة".

Ledger tab (53009–53136): entry form labels "النوع", "المبلغ", "الوصف"; type values expense label "نفقة", income label "دخل" (default expense); amount placeholder "0"; description placeholder "مثال: إيجار المكتب، رواتب، دخل خدمات..."; button "تسجيل" (disabled without numeric amount, trimmed description, or while pending). Ledger table columns "التاريخ", "النوع", "الوصف", "المبلغ", blank action. Rows: La(ie.entryDate); income badge "دخل", expense badge "نفقة"; ie.description; nt(Number(ie.amount)); delete aria-label="حذف". Empty title "لا توجد قيود بعد", hint "سجّل النفقات والدخل العام للمكتب هنا". Success toast "تم تسجيل القيد"; delete success "تم حذف القيد".

Summary tab (53137–53207): columns "الشهر", "عدد المعاملات", "مبيعات المعاملات", "ربح المعاملات", "دخل آخر", "النفقات", "الصافي". Row fields exact: ie.month, ie.txCount, nt(ie.txSales), nt(ie.txProfit), nt(ie.otherIncome), nt(ie.expenses), nt(ie.net), net green if >=0 else destructive. Empty title "لا توجد بيانات بعد", hint "الملخص يُبنى تلقائياً من معاملاتك وقيودك".

Agent-names autocomplete: separate transaction form mge lines 50821 onward queries queryKey ["agent-names"], queryFn rB, staleTime 60000; rB calls GET /api/statement/agent-names (48369–48371). The autocomplete/list is rendered with datalist id/list "agent-names-list" in one form (51232–51236), and a variant id/list "agent-names-list-v" (51835–51839); exact selected field is transaction form `agent` (default value "").