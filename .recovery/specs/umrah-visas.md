Complete spec from .recovery/app.pretty.js:

UMRAH page `عملاء العمرة` (.recovery/app.pretty.js:50969+): subtitle `إدارة بيانات المعتمرين وحالات التأشيرات الخاصة بهم.`; add button `إضافة عميل جديد`; empty strings `لا يوجد معتمرون`, `أضف أول معتمر بالضغط على زر «إضافة عميل جديد».` Search is one text input placeholder `بحث بالاسم، الجواز، أو الجوال...`, passed as GET `/api/umrah?search=<value>` (URLSearchParams; omitted when empty). No page-number/pagination controls: all returned rows map with 1-based displayed `م` index.
Columns exact order: `م`, `العميل`, `الوكيل`, `الإصدار`, `الشراء`, `البيع`, `الربح`, `الإرسال`, `داخل المملكة`, `إجراءات`. Client cell shows `clientName`, then `passportNumber | phone`; issue cell formats issue date and second line `{stayDuration} يوم | {issuingAuthority}`; monetary cells format purchasePrice/salePrice/profit.
Form title add/edit: `إضافة معتمر جديد` / `تعديل بيانات المعتمر`; fields in order: `اسم العميل` text required (error `الاسم مطلوب`); `رقم الجواز` text required, dir=ltr (error `رقم الجواز مطلوب`); `رقم الجوال` text required, ltr (error `رقم الجوال مطلوب`); `الوكيل` text required with datalist agents, placeholder `اختر من الوكلاء أو اكتب اسماً` (error `الوكيل مطلوب`); `تاريخ الإصدار` date required, default today yyyy-MM-dd (error `تاريخ الإصدار مطلوب`); `مدة الإقامة (يوم)` number required, default 90 (error `المدة مطلوبة`); `تاريخ دخول المملكة (اختياري)` date optional, default empty; `جهة الإصدار` text required, default `اليمن` (error `جهة الإصدار مطلوبة`); `جهة المعاملة (تظهر في السند)` text optional, placeholder `اسم جهة المعاملة`; `حالة الإرسال` select required/default `قيد الانتظار`, options `قيد الانتظار`, `تم الإرسال`, `تم التسليم`; `سعر الشراء` number required/default 0, min 0 (error `يجب أن يكون رقماً`); `سعر البيع` number required/default 0, min 0; `ملاحظات` text optional. Buttons `إلغاء`, `حفظ البيانات`.
Umrah row actions: `تعديل`, `طباعة سند` href `/receipt/{id}?type=umrah`, `تذكير واتساب`, `حذف`. Delete uses browser confirm exact `هل أنت متأكد من حذف هذا السجل؟`; successful toast `تم الحذف بنجاح`. Entry absent shows badge `خارج المملكة` and button `تسجيل الدخول`; clicking PUT `/api/umrah/{id}` `{entryDate:new Date().toISOString()}`, toast `تم تسجيل دخول المملكة` / `بدأ العدّ التنازلي لمدة 90 يوماً`. Present entry shows counter and `دخل: {formatted date}`. Counter: if entryDate, expiry = entryDate + 90 days; otherwise issueDate + stayDuration days; difference rounded days vs local today at midnight. null if invalid. `تجاوز {abs(days)} يوم` red for <=0; `متبقٍ {days} يوم` red for 1..10; green for >10. Send-status colors: `تم التسليم` blue, `تم الإرسال` amber, `قيد الانتظار` gray.
Umrah WhatsApp: configured template `whatsappUmrahTemplate.trim() ||` default exact: `السلام عليكم ورحمة الله،\nمن {office}.\nنذكّركم بقرب انتهاء مدة إقامة العمرة الخاصة بالمعتمر: {name}.\nالمتبقّي: {days} يوماً.\nنرجو التكرم بمراجعتنا لإتمام إجراءات المغادرة في الوقت المحدد.\nشاكرين لكم حسن تعاونكم.` Variables substituted by `{office}` officeName or `مكتبنا`, `{name}` clientName, `{days}` counter or `غير محدد`; unresolved variables become empty. Opens `https://wa.me/{phone digits only}?text={encodeURIComponent(message)}` in new tab, then PUT `/api/umrah/{id}` `{sendStatus:"تم الإرسال"}`.

OTHER VISAS page `تأشيرات أخرى` (.recovery/app.pretty.js:51551+): subtitle `إدارة بيانات تأشيرات الزيارة والعمل والتأشيرات المتنوعة الأخرى.`; add `إضافة تأشيرة جديدة`; empty `لا توجد تأشيرات`, hint `أضف أول تأشيرة بالضغط على زر «إضافة تأشيرة جديدة».`; search placeholder `بحث بالاسم، الجواز، الطلب، أو الجوال...`, GET `/api/visas?search=<value>`; no pagination controls, 1-based `م` index.
Columns exact order: `م`, `العميل`, `رقم الطلب`, `الوكيل`, `الإصدار`, `نوع التأشيرة`, `الشراء`, `البيع`, `مستلم (عميل)`, `باقي (عميل)`, `محول (وكيل)`, `باقي (وكيل)`, `الربح`, `إجراءات`. Client cell includes green WhatsApp icon (aria-label `تم إرسال تذكير`) iff sendStatus=`تم الإرسال`; secondary passport/phone. Issue cell date + issuingAuthority. Monetary values purchasePrice, salePrice, receivedFromClient, clientBalance, transferredToAgent, agentBalance, profit.
Other form title `إضافة تأشيرة جديدة` / `تعديل التأشيرة`; fields: `اسم العميل` text required; `رقم الجواز` text required ltr; `رقم الطلب` text required ltr; `رقم الجوال` text required ltr; `الوكيل` text required datalist placeholder `اختر من الوكلاء أو اكتب اسماً`; `تاريخ الإصدار` date required default today; `نوع التأشيرة` required select default `زيارة عائلية`, placeholder `اختر النوع`, options exact `زيارة عائلية`, `زيارة شخصية`, `تأشيرة عمل`, `علاج`, `سياحة`, `دراسية`, `أخرى`; `جهة الإصدار` text required default `السعودية`; `جهة المعاملة (تظهر في السند)` optional placeholder `اسم جهة المعاملة`; `سعر الشراء` number required default 0 min 0; `سعر البيع` number required default 0 min 0; `مستلم من العميل` number required default 0 min 0; `محول للوكيل` number required default 0 min 0; `ملاحظات` text optional. Validation errors exact: `الاسم مطلوب`, `رقم الجواز مطلوب`, `رقم الطلب مطلوب`, `رقم الجوال مطلوب`, `الوكيل مطلوب`, `تاريخ الإصدار مطلوب`, `نوع التأشيرة مطلوب`, `جهة الإصدار مطلوبة`, `يجب أن يكون رقماً`. Buttons `إلغاء`, `حفظ البيانات`.
Other actions: `تعديل`, `طباعة سند` href `/receipt/{id}?type=visa`, `رسالة واتساب`, `حذف`; same delete confirm and success toast. WhatsApp uses configured `whatsappOtherTemplate.trim() ||` default exact `السلام عليكم ورحمة الله،\nعزيزنا {name}،\nمعكم {office}.\nبخصوص تأشيرتكم من نوع ({visaType})، نرجو التواصل معنا لأي استفسار أو لاستكمال الإجراءات.\nشاكرين لكم.`; variables `{office}` officeName or `مكتبنا`, `{name}`, `{visaType}`; opens wa.me digits-only URL and PUT `/api/visas/{id}` `{sendStatus:"تم الإرسال"}`. No days counter/entry-date/status flow on other-visas page.
API methods: Umrah GET `/api/umrah` (query object includes `search`), POST `/api/umrah`, PUT/DELETE `/api/umrah/{id}`. Visas GET `/api/visas` (search), POST `/api/visas`, PUT/DELETE `/api/visas/{id}`. Offline create only: when navigator.onLine false, enqueue localStorage key `oboor-outbox-v1` record `{id,kind:"umrah"|"visa",payload,label:clientName,queuedAt}`; toast exact `حُفظت مؤقتاً على الجهاز` / `ستُرفع تلقائياً عند عودة الإنترنت`. Sync POSTs `/api/umrah` or `/api/visas` with payload plus `clientRequestId:r.id`; successful removes outbox item; non-auth 4xx except 429 removes and records error; network/401/429 stops. Updates/deletes are not queued offline.